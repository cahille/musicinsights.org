class Delta:
    def __init__(self, delta, note, streamIndex, measure, beat):
        self.delta = delta
        self.note = note
        self.streamIndex = streamIndex
        self.measure = measure
        self.beat = beat
